# Local only: keep serviceAccountKey.json in this folder (gitignored).
# On Vercel: paste the full service account JSON as one line:
FIREBASE_SERVICE_ACCOUNT_JSON=
STORAGE_BUCKET=
CLIENT_URL=http://localhost:5173
